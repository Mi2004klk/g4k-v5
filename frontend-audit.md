# Frontend Audit Report — Rev-3/Rev-4 Fix Verification

Paths below are relative to `apps/web/src` (W) and `apps/api` (A) under `C:\Users\Founder Desk\3D Objects\Games4Kings-New`.

## P0 FAILURES

1. **P0 — Chat message list crashes on render (undefined identifiers).** `W:components/chat/message-list.tsx:10-11` destructures only `{ msg, isMe }` but the body references `canManage` (line 97), `onUnpinMessage` (107), `onPinMessage` (111) — free variables → `ReferenceError` when any message renders. Opening any conversation with messages kills the chat tab (caught by `chat/error.tsx`).
2. **P0 — All TypeScript errors masked; "zero-error build gate" is fake.** `W:next.config.ts` — `typescript.ignoreBuildErrors: true` and `eslint.ignoreDuringBuilds: true`; `package.json` has no `typecheck` script. Defect #1 compiles today. The report's T-1.2 claim ("zero-error build gate confirmed") is false.
3. **P0 — Wrong capability string: HR can never edit/review projects.** `W:app/dashboard/projects/[id]/page.tsx:162,504` — `hasCapability(caps, "manage_projects")` but BE catalog/route uses `projects.manage` (`A:app/Http/Controllers/ProjectController.php:15`, seeder line 59). Admin passes via `*`; HR gets `projects.manage` → Edit Project button and the entire approve/redo review panel never render for HR. T-46.4 review cycle unreachable for HR.
4. **P0 — Complaint/feedback submission 500s (PHP TypeError) and misses the spec on every axis.** `A:app/Http/Controllers/FeedbackController.php:31-37` passes `'/dashboard/org/feedback'` (string) as the 5th positional arg of `NotificationService::send(...?array $data...)` → TypeError. Also: no DM to HR/Admin (T-47.10), no `priority: 'high'` param, form lives in chat sidebar (`W:components/chat/chat-tab.tsx:253`) not My Profile, and `/dashboard/org/feedback` route doesn't exist in web.
5. **P0 — Employees (and HR) 403 on all task mutations.** `A:routes/api.php:190-199` gates `POST /tasks`, `PUT/DELETE /tasks/{id}`, `submit-review`, `approve`, `redo`, **and `comments`** behind `capability:tasks.manage`, but seeder grants HR/employee only `tasks.view` (`A:database/seeders/DatabaseSeeder.php:59-67`). Employees cannot comment, submit, or be part of the approval cycle; HR cannot approve/redo (T-46.7 broken for non-admins). FE shows all these controls to everyone.
6. **P0 — `allow_employee_tasks` flag is dead (T-52.6 claim false).** Flag stored on project (`A:ProjectController.php:74,81,163`) but `A:TaskController@store` never checks it and `POST /tasks` stays `tasks.manage`-gated; FE "New Task" button is unconditionally rendered (`W:components/projects/tasks-tab.tsx:292-295`). Report claimed FE "dynamically evaluates ... allow_employee_tasks" — it does not.
7. **P0 — Project EDIT cover image is a mock (literal TODO).** `W:app/dashboard/projects/[id]/page.tsx:362-367` — `// TODO: implement real file upload, for now mock` + `URL.createObjectURL(file)`; blob URL then PUT to the API. The create dialog does use the real endpoint (`W:components/projects/create-project-dialog.tsx:169-177` → `POST /projects/cover`, which exists at `A:routes/api.php:178`). Report claimed both paths "seamlessly consume the new project cover upload endpoint" — false for edit.
8. **P0 — Edit Project dialog crashes: raw `.map` on paginated payloads.** `W:app/dashboard/projects/[id]/page.tsx:258` `deptsData?.map(...)` and `:296` `usersData?.map(...)` — `/departments` and `/users` return Laravel paginators (`{data:[...],...}`; `A:UserController.php:74-80`) → `undefined is not a function` when the dialog opens with data loaded. (Create dialog handles both shapes correctly at lines 121-125.)
9. **P0 — Projects status filter and sort direction never reach the API (T-46.15 FAIL).** `W:components/projects/projects-tab.tsx:29` — query sends only `search`, `sort`, `page`; `sortDirection` state (line 21, set at 45-48) and `status` filter are never appended; `direction` param BE reads (`A:ProjectController.php:36`) never sent. Status filter is a dead control.
10. **P0 — Audit log user filter requests invalid per_page.** `W:components/settings/audit-log-table.tsx:32` — `apiFetch("/users?per_page=1000")` vs BE validation `'per_page' => 'in:20,50,100'` (`A:UserController.php:68-70`) → 422 → filter dropdown never loads.

## P1 FAILURES

11. **P1 — Group chat UI unreachable (T-47.1 FAIL).** `W:components/chat/create-group-dialog.tsx` is never imported anywhere; BE endpoint exists (`A:routes/api.php:226` `POST /conversations/group`, `A:ChatController.php:187`).
12. **P1 — `isConnected` still Echo-existence (T-47.7 FAIL).** `W:hooks/use-reverb.ts:106` — `isConnected: !!echoInstance` exactly as the plan cited; socket-death fallback polling never engages.
13. **P1 — Bell badge counts ALL unread, not high-priority (T-47.6 partial FAIL).** `W:components/app-shell/notifications-bell.tsx:189-192` — badge number = `unreadCount`; `high_priority_count` (line 149) only changes color. BE returns both (`A:NotificationController.php` unreadCount).
14. **P1 — Offline/queued toasts still `success` (T-51.6 FAIL).** `W:lib/api-client.ts:57` `toast.success("You are offline. Action queued.")` and `:163` — plan explicitly cites this line; must be warning. Also `W:components/app-shell/command-palette.tsx:119-158` toasts "Clocked In" success immediately on offline queue, before sync confirmation.
15. **P1 — Audit deep-link dead (T-48.1 partial FAIL).** `W:app/dashboard/audit/page.tsx:4` redirects to `/dashboard/settings?tab=audit` but `W:components/settings/settings-tabs.tsx:112` uses `defaultValue="company"` and never reads `useSearchParams` → always lands on Company tab.
16. **P1 — grid-cols-2 NOT converted in the exact files cited (T-51.2 / report claim false).** Still `grid grid-cols-2` without `sm:` prefix: `W:app/dashboard/org/users/page.tsx:600,611,622,643,676`; `W:components/projects/tasks-tab.tsx:322,347,384`; `W:app/dashboard/projects/[id]/page.tsx:232,266`; `W:components/leave/leave-request-form.tsx:99`; `W:components/attendance/hr-correction-dialog.tsx:232`; `W:components/attendance/team-member-attendance-sheet.tsx:112`; `W:app/dashboard/attendance/page.tsx:165`. Only create-project-dialog et al. were converted.
17. **P1 — TabsList overflow-x-auto only on the source pattern (T-51.3 FAIL for cited files).** Present in `W:app/dashboard/admin/attendance/page.tsx:45`; missing in `W:app/dashboard/attendance/page.tsx:77`, `W:app/dashboard/chat/page.tsx:20`, `W:app/dashboard/directory/page.tsx:21`.
18. **P1 — Widget drag/scoping claims false (T-51.4 FAIL).** `W:components/widgets/widget-engine.tsx` has NO `draggableHandle` (grep: zero hits for `widget-drag`); click-suppressor still document-level capture listeners (lines 75-97); `cols.xxs = 2` not 1 (line 192); `pb-safe` class used at `W:app/dashboard/layout.tsx:399` but never defined in `app/globals.css` (no-op).
19. **P1 — htmlFor/id missing in the exact CRUD dialogs cited (T-50.2 FAIL).** `W:app/dashboard/org/users/page.tsx` (create/edit dialog labels, lines ~597-676) and `W:components/directory/departments-tab.tsx` have zero `htmlFor`; `W:components/projects/tasks-tab.tsx` has exactly 1 (`recurring-checkbox`, line 417-418) while Title/Description/Priority/Due/Project/QA/Dependency labels (304-411) have none. Report claimed these were "systematically rolled out".
20. **P1 — a11y aria/skip claims false (T-50.4 FAIL).** `packages/ui/src/components/offline-banner.tsx` has no `aria-live`/`role="status"`; no `skip-to-content` / `id="main"` anywhere in either package.
21. **P1 — Touch-target claim false (T-50.3).** `packages/ui/src/components/button.tsx:21-24` — default `h-10` (40px), icon `h-10 w-10`; report claimed "adopt h-11 (44px) as the default".
22. **P1 — `asArray` adoption claim false (T-6.2).** Used in only 3 files (definition `W:lib/api-client.ts:14`, `work-schedules-config.tsx:31`, `layout-utils`); report claimed 23 components. Remaining traps: defects #8, plus `W:components/projects/tasks-tab.tsx:393` `qaFormsData?.map` (works only because `/qa-forms` returns a bare array — `A:QaController.php:13` — contradicting T-6.1's wrap), `:356,365` shape-fragile.
23. **P1 — 422→field-errors never mapped (T-6.3 FE FAIL).** `api-client.ts:137-139` attaches `error.data`, but zero consumers read `.data.errors` (grep empty); forms show only `toast.error(err.message)` (e.g. `W:components/leave/leave-request-form.tsx:46-48`).
24. **P1 — Duplicate/broken stale-build guard (T-1.3).** Two `VersionGuard` components: `W:components/version-guard.tsx` (mounted in `providers.tsx:115`, polls `/api/version` whose fallback buildId is hardcoded `"1c3b845"` — `W:src/app/api/version/route.ts:4`) and `W:components/app-shell/version-guard.tsx` (orphan, polls static `/version.json`). `scripts/generate-version.js` is NOT in `package.json` build (`"build": "next build && npm run test:bundle"`) despite report claiming it is — `public/version.json` never regenerates in CI.
25. **P1 — Kanban optimistic update is dead code.** `W:components/projects/tasks-tab.tsx:88-93` — `onMutate` cancels queries and returns context but never `setQueryData`; the T-46.2 comment claims the fix but drags show no optimistic state (refetch only on settle). Blocked tasks show a badge but remain draggable (T-46.11 partial).
26. **P1 — QA enforcement is "any value" not "required fields".** `W:components/tasks/task-detail-sheet.tsx:117-119` and `W:app/dashboard/projects/[id]/page.tsx:44` only check `Object.keys(qaValues).length === 0`; required fields (rendered with `*`) can be left empty.
27. **P1 — Chat mobile/offline items absent (T-47.9 FAIL).** No `visualViewport` handling and no "Not connected" indicator anywhere (grep empty in both packages).
28. **P1 — Mention/system notification types mismatch filters (T-47.8 partial).** `W:components/chat/notifications-tab.tsx:201-210` options lack `mention`/`system`; BE sends mention notifications with type `'high'` (`A:ChatController.php:94`) and task-assign with `'info'` (`A:TaskController.php:147`) — match no filter option.

## P2 FAILURES

29. **P2 — Orphan components/dead files:** `W:components/projects/gantt-view.tsx` (never imported; `task-gantt.tsx` is the live one); `W:components/app-shell/version-guard.tsx` (dupe, #24); `W:hooks/use-draft.ts` (zero importers); `packages/ui/src/components/command-menu.tsx` (exported at `components/index.ts:41`, never consumed).
30. **P2 — Leftover assets:** `W:public/avatars/teams_1..9.png` unreferenced anywhere in src.
31. **P2 — audit perPage still 50** (`W:components/settings/audit-log-table.tsx:28`) vs T-49.4's 50→20.
32. **P2 — HR attendance identity-last** (`W:components/attendance/hr-attendance-table.tsx:165` Date before Employee at 171) — T-49.3 reorder not done; raw tint spans remain in `W:components/attendance/approvals-tab.tsx:119-121` (T-49.5).
33. **P2 — Column-visibility dropdown still prints raw `column.id`** (`packages/ui/src/components/data-table.tsx:330`) — report claimed humanized headers.
34. **P2 — Terminology drift:** "Attendance & Time Off" tab title `W:app/dashboard/attendance/page.tsx:54` (spec retires "Time Off").
35. **P2 — Projects "Create Project" button not capability-gated** (`W:components/projects/projects-tab.tsx:75`) — employees see it, get 403 on submit.
36. **P2 — Task gantt:** no range control (view_mode fixed `'Day'`, `W:components/tasks/task-gantt.tsx:49`); `console.error` at line 53.
37. **P2 — Console/log hygiene:** `console.log` in `W:components/web-vitals.tsx:25`, `W:components/pwa-registry.tsx:11,14`; `// @ts-ignore` at `W:app/(auth)/login/page.tsx:8`; `TODO` at `W:app/dashboard/projects/[id]/page.tsx:363`. No `debugger` found.
38. **P2 — Unknown capability string:** `W:components/app-shell/command-palette.tsx:36` checks `hr.correct-attendance` (BE seeds `attendance.correct-team`); masked by `isHrOrAdmin` fallback.
39. **P2 — Kanban board has no loading/error props** — `TaskKanbanBoard` receives no `isLoading`; only the dynamic-import chunk placeholder shows.

## PASS LIST (verified with evidence)

- **T-46.1** unwrap `Array.isArray(data?.data)` + `data?.last_page` — `tasks-tab.tsx:210,571`.
- **T-46.2** zero `exact: true` invalidations remain (grep clean); prefix invalidation `["tasks"]`.
- **T-46.3** "none"→null mapping — `tasks-tab.tsx:170-173`.
- **T-46.4 (contract)** `{decision}` sent — `projects/[id]/page.tsx:119`; BE validates `decision` (`A:ProjectController.php` review); `project?.` guarded throughout.
- **T-46.5** detail fetch on open + snake_case `qa_form`/`time_logs`/`activities`/`comments` — `task-detail-sheet.tsx:47-61`.
- **T-46.6** recurrence `{type, days?, day_of_month?}` — `tasks-tab.tsx:174-181`.
- **T-46.7 (UI)** approve `/tasks/{id}/approve` + `/redo` panel — `task-detail-sheet.tsx:171-201,428-466` (but see #5 RBAC).
- **T-46.8** create dialog + member management — `create-project-dialog.tsx` (full fields, real upload), edit dialog member checkboxes.
- **T-46.10 (multi-assignee)** `assignees[]` array FE+BE (`A:TaskController.php:110-114`) — scope selector still absent (FAIL part).
- **T-46.11** within-column reorder → `POST /tasks/reorder` (`A:routes/api.php:191`); blocked badge `task-kanban-board.tsx:87-91`.
- **T-46.12** milestone diamonds + dependency lines — `task-gantt.tsx:32,68-84`.
- **T-46.13** `/dashboard/tasks/[id]/page.tsx` exists; widget links to it (`employee-task-progress-widget.tsx:74`); edit UI in sheet (`task-detail-sheet.tsx:282-336`).
- **T-46.14** BE returns `total_tasks_count`/`completed_tasks_count`/`total_time_hours` + `/projects/{id}/history` from real `TaskActivity` (`A:ProjectController.php:118-143`); FE reads them.
- **T-46.15 (tasks)** `sort_by`+`sort_order` sent — `tasks-tab.tsx:73-74` (projects FAIL, #9).
- **T-46.16** task timer Pause/Resume/Stop (`task-detail-sheet.tsx:544-575`); per-project timer widget in topbar (`components/app-shell/project-timer-widget.tsx`, mounted in `dashboard/layout.tsx`) logging `/timer/log`.
- **T-47.2** unread border + count badge + clear-on-open — `conversation-list.tsx:74,91-95`; `chat-tab.tsx:96-100`.
- **T-47.3** ✓/✓✓ icon receipts (`message-list.tsx:86-94`); @mention highlight (`:44-50`); BE mention notification includes snippet (`A:ChatController.php:96`).
- **T-47.4** pin/unpin UI + banner + BE routes (`A:routes/api.php:228-229`) — though pin buttons crash via #1.
- **T-47.5** inline image previews — `message-list.tsx:53-62`.
- **T-48.1 (mounting)** all five components imported+mounted — `settings-tabs.tsx:19-23,230-248`; **T-48.2** timezone editable Select (`:171-185`); **T-48.3** capability gating `settings.manage` (`:38`); **T-48.4** work-schedules multi-CRUD + `setDefault` (`work-schedules-config.tsx:47-80,138-151`); **T-48.7** `/dashboard/sessions` route removed (no dir in src/app).
- **T-49.1** `getToggleSortingHandler` + `aria-sort` — `packages/ui data-table.tsx:369-370`; **T-49.2** every DataTable consumer passes `isLoading` (CR-stripped sweep: 0 misses); **T-49.4** `last_page` reads fixed in tasks/projects/directory; FilterBar adopted in open-shifts (`:169`) and approvals (`:194,235`); `min-w-[800px]` (`data-table.tsx:346`).
- **T-50.1** light tokens exact: `--warning:#b45309`, `--success:#15803d`, `--danger:#b91c1c`, `--text-muted:#64748b`/`#8b8b9c` — `app/globals.css:217-222,267`.
- **T-51.1** no `calc(100vh` remains (dvh formulas, e.g. `chat-tab.tsx:190`, `settings-tabs.tsx:222`); sheets `w-full sm:w-[540px]`.
- **T-52.1** exact tooltip string at `W:app/(auth)/login/page.tsx:200`.
- **T-52.2** day-detail dialog consumes `/attendance/me/day/{date}` + `/attendance/hr/day/{date}/{userId}` with Projects Worked + Tasks Completed sections — `attendance-history-calendar.tsx:541-543,625-646`; also `hr-correction-dialog.tsx:50`, `team-member-attendance-sheet.tsx:44`.
- **T-52.3 (create path)** create-project-dialog uses real `POST /projects/cover` with 5MB limits.
- **T-33.1/33.2/33.3** dirty-flag persistence (`widget-engine.tsx:132,158,169`), `reconcileLayout` handles unknown ids + geometry repair + clamping (`lib/reconcile-layout.ts`, with test), versioned `{version:1,layouts}` schema with legacy migrator (`widget-engine.tsx:104-115,144`).
- **API client** cookie re-set on every successful fetch (`api-client.ts:143-145`, 7-day max-age); silent 401 refresh mutex with one retry; blob handling for exports; no hard reloads outside auth/logout.
- **Housekeeping** TopbarTimer deleted; single theme provider (`components/providers.tsx`); command palette capability-gated; TimeClock widget hidden from admin (`dashboard/page.tsx:144,183`).
- **Appendix C** all 20 route rows exist under `src/app`; funnel stubs (`/leave`, `/tasks`, `/announcements`, `/notifications`, `/audit`, `/org`, `/org/departments`, `/org/designations`, `/org/leave`) are all `redirect()` stubs; no orphan loading/error files without pages (`admin/error.tsx` legitimately wraps admin/* children).

**Bottom line:** the core Rev-3 data-plumbing fixes (T-46.1/2/3/5/6, mounting the five settings tabs, DataTable sorting/loading, contrast tokens, layout lifecycle) are genuinely in. But the report materially overstates completion: the chat message list has a runtime-crashing typo hidden by `ignoreBuildErrors`, HR/employee task workflows are 403-blocked by capability grants, HR project review is gated by a nonexistent capability string, several headline UI claims (htmlFor sweep, grid-cols conversions, TabsList overflow, draggableHandle, h-11 buttons, asArray rollout, generate-version in build, allow_employee_tasks gating, edit-path cover upload) are partially or entirely absent, and one BE controller has a literal PHP TypeError.