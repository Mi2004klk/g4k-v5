# Tasks — system-settings

- [ ] 1. [spec] Extend OpenAPI: company profile get/put + logo upload, settings get/put by category, holiday CRUD, audit-log query + export. Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `SettingsClient` methods (company profile, settings by category, holidays, audit logs) in shared API client.
- [ ] 3. [api] Migration: create `company_profile` singleton (name, short_name, logo_url, timezone, branding json, updated_by, timestamps) + a one-row seed guard.
- [ ] 4. [api] Migration: create `settings` (category, key, value json, updated_by, timestamps); unique on (category, key); read-through cache helper.
- [ ] 5. [api] Migration: create central `audit_logs` (user_id, action, subject_type, subject_id, before json, after json, ip, meta json, at) + indexes; data-move step to port earlier per-phase audit rows; drop/alias old tables.
- [ ] 6. [api] Install/configure central activity logger (spatie/laravel-activitylog or custom) writing to `audit_logs`; diff helper for before/after; IP + actor capture.
- [ ] 7. [api] Wire module events to the logger: auth (login/lockout/suspicious/revoke), org (user/dept/designation CRUD + password reset), attendance (manual correction), leave (submit/approve/reject), projects/tasks (create/assign/submit/approve/redo/archive/delete), settings/company/holiday writes.
- [ ] 8. [api] `GET/PUT /company/profile` (singleton load/save); `POST /company/logo` → store + optimize + size-limited → return `logo_url`.
- [ ] 9. [api] `GET/PUT /settings/{category}` with cache invalidation on write; categories: company, working-hours, policies, sessions, notifications, reminders.
- [ ] 10. [api] Holiday CRUD (R2.9 master-data pattern): name, date, description; reuse Phase 6 `holidays` table.
- [ ] 11. [api] Enforce policies: password min length + expiry (force change on sign-in when expired); session timeout + max devices per user (revoke oversubscribed sessions on rule change).
- [ ] 12. [api] Apply notification preferences + reminder-time config: route/suppress events per Phase 8 toggles; scheduler reads reminder offsets (attendance shift/late, task due).
- [ ] 13. [api] `GET /audit-logs` (filter by user/action/subject_type/date range, paginated) + `GET /audit-logs/export` (xlsx/pdf via Phase 9 exporter); capability-gated.
- [ ] 14. [api] Add capabilities `admin.settings.manage`, `admin.audit.view` to the matrix; attach to Super Admin role.
- [ ] 15. [web] Company Profile UI: logo upload (format + size limits, preview), name, short name, timezone picker, branding editor; reflect logo/name/timezone app-wide.
- [ ] 16. [web] Working Hours config UI: per-weekday start/end/break editor with grace + overtime thresholds; live preview.
- [ ] 17. [web] Holiday Calendar manager: month grid + add/edit/delete; read-only badge noting Attendance/Leave consume it.
- [ ] 18. [web] Settings forms: password policies (min length, expiry), session/device rules, notification preferences (per-event toggles), configurable reminder times — sectioned, save-as-draft + autosave + restore banner (Phase 3 form system).
- [ ] 19. [web] Audit Log viewer: filter bar (Phase 3), before/after diff viewer, pagination, export button; capability-gated; virtualized list (R11.5).
- [ ] 20. [deploy][api] Wire Sentry Laravel SDK: DSN env, release tag = git SHA, secret scrub, breadcrumbs; gate Pulse dashboard behind `admin.settings.manage`.
- [ ] 21. [deploy][web] Wire Sentry browser SDK + source-map upload on Vercel build; configure Laravel Pulse ingestion.
- [ ] 22. [test] Perf audit: run bundle analyzer; verify lazy routes + separate vendor bundle + ECharts code-split; fix over-budget chunks.
- [ ] 23. [test] Confirm virtualization on employees, attendance logs, tasks, notifications, reports, audit-log lists (R11.5); spot backend N+1 + add eager loads/indexes; enable route/config cache + OPcache on prod.
- [ ] 24. [test] Run Lighthouse on key routes (sign-in, dashboards, attendance, tasks, reports, audit log) + Core Web Vitals (LCP/INP/CLS) vs §19 targets; capture artifact; fix regressions to green.
- [ ] 25. [perf] Virtualize the audit-log viewer (`@tanstack/react-virtual`); add composite indexes on `audit_logs` `(subject_type,subject_id,at)`, `(user_id,at)`, `(at)`; add a query-count feature test asserting `GET /audit-logs` executes ≤ 5 SQL with zero N+1 at 10k+ rows (R13.5/6/14).
- [ ] 26. [perf] Move central activity-logger writes to an async/queued path so audited operations (create/approve/delete across modules) are not slowed by audit persistence; on audit-write failure report to Sentry/Pulse without failing the user's operation (R13.17).
- [ ] 27. [perf] Implement settings/company-profile read-through server cache with write invalidation + client `staleTime` 1h for static config entities (R13.10); ensure holiday calendar + working-hours cache is reused by Attendance reminders/reports without refetch.
- [ ] 28. [perf] M1 performance-freeze audit: Lighthouse CI green across ALL primary routes (not just a sample); enforce bundle budget ≤ 200KB gz/route via `@next/bundle-analyzer` failing the build on breach; verify field web-vitals p75 (LCP/INP/CLS) meet targets for 7 consecutive days (R13.28/29).
- [ ] 29. [perf] Review the TRACKER performance breach log at freeze time — confirm every entry is resolved or carries an owner + documented plan; record the final verdict in the archive notes.
- [ ] 30. [perf][monitor] Verify production monitoring is live and reporting: Sentry (errors+perf) release-tagged in api+web, Laravel Pulse dashboards capturing slow requests/queries/jobs, and field web-vitals (LCP/INP/CLS) flowing to the analytics dashboard; assert dashboards show p75 within targets (R13.28).
- [ ] 31. [seed] Seed: company_profile row, default settings per category, default holidays, baseline reminder offsets.
- [ ] 32. [test] Full phase test pass: api feature tests (profile/settings/holiday/audit capture+filter+export/policy enforcement/reminder offset) + web tests (forms, audit viewer, capability gate) + perf artifact recorded.
- [ ] 33. [deploy] Staging deploy; smoke each settings screen, confirm audit captures create/approve/delete, confirm Sentry error + Pulse slow-request capture; verify rollback + Supabase backup restore drill.
- [ ] 34. [deploy] Deploy production; promote; verify Sentry release-tagged errors + Pulse live; verify perf targets met in prod.
- [ ] 35. [docs] Archive Phase 10 via `/opsx:archive`; mark **M1-FREEZE** milestone in TRACKER (all phases 0–10 ✅, perf targets met, seeded, monitored, deployed with rollback + backups verified).
