# Tasks — communication

- [ ] 1. [spec] Extend OpenAPI: conversations (list, create group, get, members, message history, send, pin, read), direct open, notifications (list, mark-read, unread-count), announcements (list, create, react, dismiss), notes (CRUD), feedback submit. Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `ChatClient` + `NotificationClient` methods in shared API client; add Tiptap mention extension.
- [ ] 3. [api] Migrations: `conversations`, `conversation_members`, `messages`, `read_receipts`, `announcements`, `notes`, `feedback_complaints`, `notifications`; indexes on `(conversation_id, created_at)` and `(user_id, read_at)`.
- [ ] 4. [api] Reverb channel authorization: `private-user.{id}`, `presence-conversation.{id}` (membership from `conversation_members`), `presence-project.{id}` (project team), `public-announcements`; capability check helpers.
- [ ] 5. [api] Seed one `global` conversation (company-wide) on first boot; enroll all users; Global Chat send/history/list endpoints.
- [ ] 6. [web] Global Chat view: realtime over presence-conversation; message list (virtualized); compose box; offline "Not connected" + pending indicator.
- [ ] 7. [api] Project Chat: auto-create conversation (type=project, project_id) on project create (Phase 7 hook); enroll team members via R7.2 grant.
- [ ] 8. [api] Task-alert auto-post: on task created/assigned/submitted/approved (Phase 7 events), insert a `system_event` message into the project conversation.
- [ ] 9. [web] Project Chat view: team-only access guard; pinned messages rail; task-alert rendering (system message styling).
- [ ] 10. [api] Direct Chat: `POST /conversations/direct/{userId}` open-or-create (sorted-pair unique key); send/history endpoints; `Employee Directory → Send Message` returns conversation id.
- [ ] 11. [web] Direct Chat view: 1:1 thread; open from directory/profile; realtime delivery.
- [ ] 12. [api] Custom Group Chats: `POST /conversations` (capability `conversations.group.create` = hr); membership = added members only; list filters by membership.
- [ ] 13. [web] Group create flow (HR): member picker, title, description; employees see only groups they're in.
- [ ] 14. [api] @mention handling: parse Tiptap mention nodes on send → write `mentions` array → dispatch `Mentioned` + create high-priority notification per target.
- [ ] 15. [web] @mention dropdown: `@` trigger → query `GET /conversations/{id}/members` (cached) → insert mention node; snippet shown in resulting notification.
- [ ] 16. [api] DM read receipts: `POST /conversations/{id}/read` writes `read_receipts` for DMs + emits `MessageRead`; sender sees receipt.
- [ ] 17. [web] DM read-receipt indicator on messages; HR pin-message control in project chats (`messages.pin` capability) → pinned rail.
- [ ] 18. [api] Read/unread state: compute unread per member (`messages.created_at > last_read_at`); mark read on conversation open.
- [ ] 19. [web] Chat list: colored border + unread count badge; clear on open; bell-independent.
- [ ] 20. [api] File/image sharing: shared attachment endpoint (mime + size config); `messages.attachments` json; task submission accepts link/directory list only (Phase 7 form integration).
- [ ] 21. [web] File/image popup (reuse dialog + profile-pic popup pattern): client mime/size validation, preview, attach to message; task-submission link fields.
- [ ] 22. [api] Offline chat: Offline Engine `chat.send` op with `client_temp_id`; dedupe on replay; authoritative id assignment; Timestamp ordering; backfill via `?after=` on Reverb reconnect.
- [ ] 23. [web] Offline queue UI: pending message state in thread; "Not connected" header; sync-on-reconnect reconciliation.
- [ ] 24. [api] Notification system: `notifications` write helpers (mention, submission, approval, announcement, holiday, feedback, suspicious_login); `GET /notifications`, `POST /notifications/read`, `GET /notifications/unread-count` (counts high + system only).
- [ ] 25. [web] Notification bell: unread count badge; dropdown history (virtualized, filterable); mark-read on open/item; `Notified` event increments live.
- [ ] 26. [web] Notification Center (inside Chat): aggregated feed of leave/task/project submissions, announcements, holiday reminders, feedback; deep-link to source.
- [ ] 27. [api] Announcement board: `POST /announcements` (capability `announcements.company.post`=admin / `announcements.team.post`=hr scoped); `GET /announcements` (company + caller's teams, pinned first); react; dismiss (per-user dashboard close).
- [ ] 28. [web] Announcement board UI: list, pin, reactions only (no comments), post composer (role-gated); dashboard pinned-announcement widget (closeable); notify on post via `AnnouncementPosted`.
- [ ] 29. [api] Quick Notes: `GET/POST/PATCH/DELETE /notes`; `pinned_to_dashboard` + `sort_order`.
- [ ] 30. [web] Quick Notes: private notes surface in sidebar + command palette; pin-to-dashboard toggle; dashboard widget renders pinned notes.
- [ ] 31. [api] Complaint/feedback channel: `POST /feedback` (to_role hr|admin) → open DM to a target HR/Admin + create high-priority global notification; record `feedback_complaints`.
- [ ] 32. [web] Feedback form on Employee Profile: private submit → confirmation; resulting DM thread opens for the recipient with high-priority notification.
- [ ] 33. [web] Mobile chat UX: list-first layout (≤ breakpoints), full-screen conversation on tap, input fixed above keyboard, back control → list; reuse responsive shell from Phase 3.
- [ ] 34. [api] Capability wiring: declare `conversations.group.create`, `messages.pin`, `announcements.company.post`, `announcements.team.post`, `feedback.submit`; attach to roles in the matrix (Phase 2 lookup).
- [ ] 35. [seed] Seeder: global conversation; a sample group (HR-created); 2 announcements (company + team); a Quick Note per seed user; a few notifications per role for demo.
- [ ] 36. [web][test] Virtualize the message history list (`@tanstack/react-virtual`) with append-only render, stable `message.id` keys, and `React.memo` rows; add a 60-FPS + DOM-node-budget test at 5000 messages and a render-count test that an appended message does not re-render siblings. (R13.14/13.12)
- [ ] 37. [web][test] Optimistic message send: instant pending-row insert, offline queue via Offline Engine (`chat.send` + `client_temp_id`), and rollback-to-failed-on-error with danger toast; test rollback path and no-duplicate delivery. (R13.19/13.20)
- [ ] 38. [web][test] Scoped Reverb subscriptions: subscribe only to the open `presence-conversation.{id}` + `private-user.{id}`, unsubscribe on conversation switch; coalesce inbound event bursts into one render flush/frame; incoming `MessageSent` patches the cache (no refetch). Add a 20-screen nav memory/subscription test (no accumulation, no detached nodes) and a render-storm burst test. (R13.26/13.12/13.10)
- [ ] 39. [web][test] Instant @mention filter: client-side filter of cached conversation members (<50ms, debounced, no per-keystroke network); optimistic mark-read on conversation open (border/badge clear) with rollback test. (R13.15/13.19)
- [ ] 40. [web][test] Cached conversation list (TanStack Query, 15s `staleTime`, bounded `gcTime`, stale-while-revalidate, no spinner on cached data) + optimistic unread decrement on read with rollback; assert stale-then-fresh render on re-entry. (R13.10/13.19)
- [ ] 41. [web][test] Virtualize the notification history list (DOM-node budget, 60 FPS at 5000 rows) + optimistic bell unread decrement on mark-read (high + system priority) with rollback test. (R13.14/13.19)
- [ ] 42. [web][test] Lazy-load the Tiptap compose editor (dynamic import + idle-prefetch, out of chat route chunk; bundle-analyzer gate) and enforce attachments via `next/image` with client-side mime/size validation before upload (no bulk attachment-blob load with the message list); add First-Load-JS budget check for the chat route. (R13.8/13.9/13.7)
- [ ] 43. [test] api feature tests (membership visibility, mention→notify, DM read receipt, pin gate, group-create gate, announcement scope gate, unread-count high+system only, feedback→DM+notify, offline dedupe, Timestamp order); contract tests; query-count tests for `GET /conversations/{id}/messages` and `GET /notifications` (≤5 SQL regardless of row count, cursor pagination); web tests (unread border/badge, mark-read, mention dropdown, image popup limits, offline indicator, bell count, reactions-no-comments, mobile flow, notes pin).
- [ ] 44. [deploy] Staging deploy; smoke: send DM + verify read receipt; @mention notifies target; post company + team announcement → dashboard + bell; create Quick Note; HR create group + verify non-member can't see it; offline a message and reconnect; verify mobile chat flow; promote production.
- [ ] 45. [docs] Archive Phase 8 via `/opsx:archive`; update tracker ✅.
