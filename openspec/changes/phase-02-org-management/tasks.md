# Tasks — org-management

- [ ] 1. [spec] Extend OpenAPI: `/users`, `/departments`, `/teams`, `/designations`, `/directory`, `/me`, `/settings/numbering-schemes`, plus import/export operations. Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `OrgClient` methods (users, departments, teams, designations, directory, me, numbering) to the shared API client.
- [ ] 3. [api] Migrations: `designations`, `departments`, `teams`; extend `users` (department_id, team_id, designation_id, phone, reports_to, employee_code, avatar_url, profile_visibility); `capabilities`, `role_capabilities`, `audit_logs`, `numbering_schemes`; create `directory_view`.
- [ ] 3a. [api] Composite indexes for common filters: `users(department_id,status)`, `users(designation_id,status)`, `users(status,employee_code)`, `role_assignments(user_id,role)`, `departments(status)`, plus index-backed columns exposed by `directory_view`. Covers every WHERE/JOIN/ORDER BY path (R13.6/P-INDEX).
- [ ] 4. [api] Capability matrix: seed `capabilities` + `role_capabilities` (super_admin/hr/employee per design); resolve active-role → capability list on the token; `GET /me/capabilities`.
- [ ] 5. [api] `require-capability` middleware applied to all org endpoints; 403 on missing capability regardless of frontend.
- [ ] 6. [api] Audit logger trait/service: append-only `audit_logs` write on every create/update/delete/status/role action with before/after, actor, ip, ua.
- [ ] 7. [api] Auto-numbering service: atomic next-seq generation per `numbering_schemes` (prefix/start/length/format); `GET/PUT /settings/numbering-schemes/{entity}` (Admin).
- [ ] 8. [api] Designations CRUD (`/designations`): list/create/update/delete (soft)/status; Admin-only (`designations.manage`).
- [ ] 9. [api] Departments CRUD (`/departments`): list/create/update/delete (soft)/archive + `GET /departments/{id}/members`; Admin-only (`departments.manage`).
- [ ] 10. [api] Teams CRUD (`/teams`) under departments; Admin-only via `departments.manage`.
- [ ] 11. [api] HR account CRUD (`/users`): create (name, email, employee_id, department, designation; auto-numbered code; must-change-password); edit; reset-password; activate/deactivate; `GET /users/{id}/activity`; capability `users.hr.manage`.
- [ ] 12. [api] Employee account CRUD: create (name, email, employee_id, department, team, designation); edit; `POST/DELETE /users/{id}/roles` (dual-role); reassign dept+team; reset-password; activate/deactivate; activity log; capability `users.employee.manage`.
- [ ] 13. [api] Master-data import/export: `POST /users/import` (multipart, summary added/updated/errored), `GET /users/export` (filtered result); same pattern wired for designations/departments.
- [ ] 13a. [api] Queue bulk import/export: when a job would exceed 500ms, dispatch to a Laravel queue (R13.17/P-QUEUE); export is produced as a streamed/Excel download by a queued job with a download-ready notification — never a long PHP request.
- [ ] 14. [api] Employee Directory: `GET /directory` (q, dept, designation, view, page) over `directory_view`; `GET /directory/{id}` public profile (respects `profile_visibility`).
- [ ] 14a. [test] Query-count + latency tests for every list endpoint (`/users`, `/directory`, `/departments`, `/departments/{id}/members`, `/designations`, `/teams`): assert p95 ≤ 200ms read, ≤ 5 SQL/request via `DB::enableQueryLog()`, zero N+1, and cursor pagination on deep pages (R13.4/5/6).
- [ ] 15. [api] Profile (`/me`): `GET/PUT` (name, phone, designation, profile_visibility); `POST /me/photo` (format+size validate + optimize); `POST /me/change-password`; expose device list + revoke (reuse Phase 1 sessions).
- [ ] 16. [api] Realtime: presence/online status (cache TTL heartbeat); broadcast `UserUpdated`/`DepartmentChanged`/`RolesChanged` on org changes; Directory + member lists refetch.
- [ ] 17. [ui-pkg] Master-data table component: configurable columns, search/filter/sort, pagination (20/50/100), activate/deactivate, import/export, audit drawer — reusable for designations/departments/teams/users.
- [ ] 18. [web] Designations, Departments, Teams screens on the master-data table component (Admin-only routes).
- [ ] 19. [web] HR management screen + Employee management screen (create/edit drawers, role assign, reset, deactivate, activity log drawer).
- [ ] 20. [web] Employee Directory: grid/list toggle, search (name/dept/designation), virtualized list, card (photo/name/designation/dept/email/phone-if-visible), click → public profile + Send Message (links to Phase 8).
- [ ] 20a. [web] Virtualize directory + Admin user tables > 100 rows (`@tanstack/react-virtual`, ≤ visible+overscan DOM nodes, 60 FPS at 5000 rows); memoize rows with stable keys, no anonymous callbacks in props (R13.12/14).
- [ ] 20b. [web] Debounce directory + master-data search 250ms server-side; filter/search/view changes update URL + cache in place (no reload, no spinner for cached data) (R13.15/3).
- [ ] 21. [web] Profile screen: editable name/phone/designation, change password, device list + remote/current logout; photo upload popup with format+size limits + preview.
- [ ] 21a. [web] Photo popup: render selected image via `next/image` with optimistic preview before upload; stored avatar ≤ 256px (display ≤ 96×96); rollback + danger toast on error (R13.9/19).
- [ ] 21b. [web] Caching config: TanStack Query staleTime per entity — designations/departments/teams/numbering-schemes 5min, directory 30s; bust affected cache keys on every mutation + rely on `private-org` broadcast (R13.10). Server-side cache numbering schemes + reference data.
- [ ] 21c. [test] Click-count + latency tests for frequent workflows: search directory (first result < 300ms, ≤ 2 clicks from dashboard), create user (≤ 2 clicks, ≤ 2 screens, no full reload, disabled+loader submit), assign department inline + bulk (optimistic, no reload) (R13.24/15/19).
- [ ] 22. [web] Auto-numbering settings screen (Admin): view/edit prefix/start/length/format per entity with live preview.
- [ ] 23. [web] Capability-gated routing + nav: hide Admin-only items for HR/Employee; offline: queue account/dept/designation edits with offline banner; server-wins reconciliation toast.
- [ ] 24. [seed] Full seed: 1 company, 2 departments, 15 designations, 13 employees + Karthik(super_admin) + Aravind(hr); branding, working days, holiday calendar, attendance rules, company docs; default passwords with must-change-password; auto-numbered IDs.
- [ ] 25. [test] api feature tests (capability gate, HR/Employee CRUD, Department/Team/Designation CRUD, auto-numbering, import/export, directory, profile/photo/password, device revoke, audit); web tests (directory, profile/photo popup, routing guards, offline queue).
- [ ] 26. [deploy] Staging deploy; smoke as karthik/aravind/praveen (create HR + Employee, dept CRUD, directory search, profile photo, reset password, dual-role Role Selection); promote production.
- [ ] 27. [docs] Archive Phase 2 via `/opsx:archive`; update tracker ✅.
