# Tasks — authentication

- [ ] 1. [spec] Extend OpenAPI: auth endpoints (login, role/select, forgot-password, reset-password, change-password, sessions, logout). Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `AuthClient` methods in shared API client.
- [ ] 3. [api] Migration: extend `users` (employee_id, must_change_password, onboarded_at, status, lockout fields, avatar_url, timestamps); create `role_assignments`, `auth_sessions`, `login_attempts`; adjust `password_resets` (channel/approval).
- [ ] 4. [api] Sanctum token issuance with device name + metadata; `personal_access_tokens` extended with device meta.
- [ ] 5. [api] `POST /auth/login`: identifier (email or employee_id) + password; rate limit; success → token + flags; failure → generic error; lockout on 5/10min.
- [ ] 6. [api] Suspicious-login detection (new device/geo heuristic); event → audit row + SMTP to HR/Admin (notification system in Phase 8).
- [ ] 7. [api] `POST /auth/role/select`: set active role on session token; reject role not assigned to user.
- [ ] 8. [api] `POST /auth/forgot-password`: smtp channel → signed reset link emailed; admin channel → row pending Admin approval.
- [ ] 9. [api] `POST /auth/reset-password`: validate token; set password; clear must_change_password; redirect to sign-in.
- [ ] 10. [api] `POST /auth/change-password`: verify current, set new, clear must_change_password.
- [ ] 11. [api] `GET/DELETE /auth/sessions`; `POST /auth/logout` (current device); remote revoke broadcasts `SessionRevoked`.
- [ ] 12. [api] `require-capability` middleware; active role → capability list lookup (matrix content in Phase 2).
- [ ] 13. [api] First-login enforcement: `must_change_password` blocks non-auth routes.
- [ ] 14. [seed] Seeder: 13 employees + Karthik(super_admin) + Aravind(hr) + others(employee); default passwords; `must_change_password=true` for seed users so first-login flow is exercised.
- [ ] 15. [web] Sign-in screen: landscape logo (from assets), welcome copy, copyright "Games4King Workplace OS", info tooltip "Gen2k Conglomerate (2018) • Milestone 1", identifier+password fields, show/hide toggle, loading + error states, forgot-password link.
- [ ] 16. [web] Role Selection screen for dual-role users → active role → dashboard route.
- [ ] 17. [web] Forgot-password flow UI (channel chooser); reset-password screen; change-password screen (first-login + profile).
- [ ] 18. [web] Onboarding welcome screen on first login.
- [ ] 19. [web] Device/session list UI; remote + current logout.
- [ ] 20. [web] Auth-aware routing: route guards by active-role capability; redirect to sign-in on 401; handle `SessionRevoked` to sign out.
- [ ] 21. [web] Offline: queue login attempt via Offline Engine; show offline banner.
- [ ] 22. [test] api feature tests (login/lockout/reset/role-select/revoke/capability gate); web tests (form validation, guards, offline-queue).
- [ ] 23. [deploy] Staging deploy; smoke sign-in as karthik/aravind/praveen; verify lockout + reset + device revoke; promote production.
- [ ] 24. [docs] Archive Phase 1 via `/opsx:archive`; update tracker ✅.
