# Tasks — projects-tasks

> Ordered, chunked (~2h each), tagged. Largest phase — covers R7.1–R7.18. Each phase ends with test + deploy + archive.

- [ ] 1. [spec] Extend OpenAPI: `/projects` (CRUD/members/team/status/archive/history/submit/approve/redo/gantt/sort), `/projects/{id}/qa-form`, `/projects/{id}/tasks` + `/tasks/{id}` (assignees/progress/status/reorder/dependencies/recurrence/submit/approve/redo), `/tasks/{id}/comments`, `/tasks/{id}/activity`, `/projects/{id}/timers`, `/quick-tasks`, `/me/tasks`, `/saved-views`, `/pins`. Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `ProjectsClient` + `TasksClient` methods (projects, tasks, members, comments, activity, timers, QA, quick-tasks, my-tasks, saved-views, pins) to the shared API client.
- [ ] 3. [api] Migrations: `projects`, `project_members` (project_id, user_id, role, granted_via), `project_chats` (project_id unique), `qa_forms`, `tasks` (scope/priority/status/order/progress/depends_on json/recurrence json/qa_submission json/approval_id/is_personal/redo_reason/version), `task_assignees`, `task_comments` (Tiptap body json), `task_timers`, `task_activity` (append-only), `task_recurrence_log`, `saved_views`, `pinned_items`.
- [ ] 4. [api] Project CRUD (`/projects`): create (name, description, priority, deadline, status, team, hr_owner, qa_form_id), edit, archive (soft), delete; Admin/HR capability `projects.manage`; audit row on each.
- [ ] 5. [api] Team auto-access: on project create/update of team, materialize `project_members` from team membership (grant project + task list + project-chat access); on team change revoke removed members. `GET /projects/{id}/members`; `PUT /projects/{id}/team`.
- [ ] 6. [api] Project sort: `GET /projects?sort=created|deadline|priority&dir=asc|desc`; persist chosen default per project via `PUT /projects/{id}/sort`. Admin sees all projects regardless of team (R7.13).
- [ ] 7. [api] Task model + create: `POST /projects/{id}/tasks` with title, description, assignees (individual/team/company-wide → resolve via team/department join), priority low/medium/high/urgent, due_date + reminder_minutes, scope global/department/role. `GET/PUT/DELETE /tasks/{id}`. Write first `task_activity` (created) row.
- [ ] 8. [api] Task dependencies: `POST /tasks/{id}/dependencies` / `DELETE /tasks/{id}/dependencies/{depId}` (HR/Admin `tasks.dependencies`); block dependent task from in_progress while predecessor not done; clear block on predecessor done; activity row on add.
- [ ] 9. [api] Task comments (Tiptap): `GET/POST /tasks/{id}/comments` (body = Tiptap doc), `PUT/DELETE /tasks/{id}/comments/{cid}`; `GET /tasks/{id}/activity` append-only log (created/assigned/progress/submitted/approved/status_changed/commented/...).
- [ ] 10. [api] Kanban reorder + status: `PATCH /tasks/{id}/status`; `PUT /tasks/{id}/reorder {source, target, position}` persisting new order within a status column; reject reorder into in_progress for dependency-blocked tasks.
- [ ] 11. [ui-pkg] Kanban board component (dnd-kit, ADR-007): four columns To Do / In Progress / Under Review / Done, cross-column + within-column drag, optimistic local reorder, blocked-card `canDrop` gate, dependency badge.
- [ ] 12. [web] Project workspace: project list (sort by created/deadline/priority asc/desc), project detail with board/list/Gantt tabs, member roster; capability-gated nav; Zustand `useTaskBoardStore` for drag/filter UI state (ADR-008), TanStack Query for data.
- [ ] 13. [web] Task list view + inline editing (R3.9 pencil→Enter/Escape): title, assignee, priority, due date, status, progress; reuses Filter/sort bar (R3.8) and pagination/virtualization (R11.5).
- [ ] 14. [api] QA form builder: `GET/PUT /projects/{id}/qa-form` (schema json: typed fields, required flags, Tiptap instructions) — HR/Admin `projects.qa.manage`; versioned.
- [ ] 15. [web] QA form builder UI (HR/Admin): compose fields (text/number/rating/choice/file-link), required toggles, Tiptap instructions field, live preview; attach to project.
- [ ] 16. [api]+[web] Employee QA submission: on task submit, render attached QA form from schema + Tiptap submission note; store responses + note as `tasks.qa_submission`; surface to reviewer in approve/redo step.
- [ ] 17. [api] Per-project work timer: `POST /projects/{id}/timers {action:start|pause|resume|end}` (one open interval per project+user; pause/resume = end+start); `GET /projects/{id}/timers` log; finalize `seconds` from `start_at`..`end_at`. Employee `timers.use`.
- [ ] 18. [web] Project timer UI: Start/Pause/Resume/End controls + live HH:MM:SS, per-project log; reuses attendance live-timer pattern (Phase 5); offline accumulates locally + finalizes on sync.
- [ ] 19. [api] Recurring tasks engine: `PUT /tasks/{id}/recurrence {type:daily|weekly|monthly, days?, dayOfMonth?, until?, active}`; `POST /tasks/{id}/duplicate-next` generates next occurrence on completion per rule; write `task_recurrence_log`; notify HR; toggle-off sets active=false. (Advanced section, collapsed by default.)
- [ ] 20. [web] Recurring tasks UI in advanced collapsed section: daily / weekly-on-days / monthly-on-date pickers, until date, active toggle; next-occurrence preview.
- [ ] 21. [api] Quick Task Assignment end-to-end: `POST /quick-tasks {assignee_id, title, due_date?, note?}` creates task in assignee's list; on task complete emit Global Chat auto-notify event + write message row; reuses Phase 4 widget action.
- [ ] 22. [web] Quick Task Assignment widget full wiring: dashboard → employee picker → create → confirm it appears in assignee's My Tasks/assigned list; verify Global Chat auto-message posts on completion (chat renders in Phase 8; assert row + event here).
- [ ] 23. [api] Task review flow: `POST /tasks/{id}/submit` (assignee; optional QA responses + note → `qa_submission`), `POST /tasks/{id}/approve` (HR/Admin → done), `POST /tasks/{id}/redo {reason}` (→ in_progress + redo_reason); reuse Phase 6 `approvals` state machine; broadcast submission to HR/Admin via Reverb.
- [ ] 24. [api] Project review flow: `POST /projects/{id}/submit` (completion report) → HR review; `POST /projects/{id}/approve`; `POST /projects/{id}/redo`; Admin sees all projects; broadcast submission notifications.
- [ ] 25. [api] Project history: `GET /projects/{id}/history` → team, tasks done, total time spent (from task_timers), completion date, approval result.
- [ ] 26. [api] Employee self-create permission + progress: per-project `tasks.self-create.grant` toggle (HR grants) → employee `tasks.self-create`; `PATCH /tasks/{id}/progress` (`tasks.update-own-progress`) → update progress_pct + activity row; deny self-create when not granted.
- [ ] 27. [api] My Tasks: `GET /me/tasks` (own + assigned across projects, personal `is_personal` tasks); `POST /me/tasks` self-create personal task (private to owner); HR/Admin can assign into a user's My Tasks (`my-tasks.assign`).
- [ ] 28. [web] My Tasks screen: personal list, self-create, progress update, status badges (Gray/Blue/Amber/Green/Red per R11.4); private to owner.
- [ ] 29. [api]+[web] Saved views + custom columns (TanStack Table): `GET/POST/PUT/DELETE /saved-views?entity=projects|tasks` (config = filters/sort/columns/groupBy/board-vs-list-vs-gantt); UI saves named view, re-applies exact columns+filters+sort; per-user.
- [ ] 30. [api]+[web] Pinning: `POST/DELETE /pins`, `GET /me/pins`; pin project/task → appears in Phase 3 Pinned sidebar section (R3.5); unpin from sidebar or item.
- [ ] 31. [web] Progress-bar animation: animate 0→value on render and on change (R3.14 motion tokens); applied to task cards, list rows, project header, Employee dashboard progress widget.
- [ ] 32. [web] Gantt/Timeline view (HR/Admin `projects.gantt`): ECharts/custom horizontal bars from `due_date`/start + milestone diamonds, dependency linkages from `depends_on`; data from `/projects/{id}/gantt`.
- [ ] 33. [api] Realtime (Reverb, ADR-013): broadcast on `private-project.{id}` `TaskCreated/TaskStatusChanged/TaskReordered/TaskUpdated` for live Kanban; auto-post project-chat task-alert system messages on create/assign/submit/approve/redo; emit `SubmissionPending` to `private-user.{hr_owner_id}` + Admin; Global Chat auto-notify on Quick-Assigned completion.
- [ ] 34. [api]+[web] Offline (ADR-009/010): tasks Version+Merge (base `version` on queued edits; merge non-conflicting fields; 409 + server copy only on same-field conflict → per-field merge affordance); projects Server Validation (submit/approve/redo require connectivity); comments Timestamp ordering; timer seconds accumulate locally; My Tasks + QA submission queue + replay; offline banner (R11.6).
- [ ] 35. [seed] Seeder: sample projects across the 2 departments with teams, multi-assign tasks across all 4 statuses, a QA form on one project, a recurring task, dependencies, timer entries, saved views, and pinned items — enough to exercise every view (board/list/Gantt/history) as karthik/aravind/praveen.
- [ ] 36. [web][test] **(perf R7.7, R13.19/14/12)** 60 FPS Kanban drag with optimistic status + debounced persist: drop flips `status` optimistically (idempotent via `client_id`); within-column reorder coalesced + debounced before the PUT; on server error the card snaps back to source column + danger toast; dnd-kit drag runs at 60 FPS. Test: React Profiler asserts sibling cards + column headers render 0 times during a drag; optimistic-rollback test (inject server error → card returns to source + danger toast).
- [ ] 37. [web][test] **(perf R13.14/12)** Virtualize the Kanban board (columns AND cards >100) with `@tanstack/react-virtual`; `React.memo` each card with stable key `task_id`; no anonymous callbacks/fresh objects in card props. Test: render a 5000-card board, assert DOM nodes ≤ (visible columns + visible cards + overscan) and 60 FPS / INP ≤200ms.
- [ ] 38. [api][test] **(perf R13.5/6/4)** Composite indexes on `tasks (project_id,status,order)`, `task_assignees (user_id,task_id)` + `(assignee_id,status)` for My Tasks, and `(priority,due_date)` for urgent/overdue sort; cursor pagination on board/list/My Tasks endpoints. Test: query-count test asserts ≤5 SQL per list request and zero N+1 (via `DB::enableQueryLog()`) at 10k tasks; EXPLAIN asserts index usage on filtered/ordered columns; p95 ≤200ms benchmark.
- [ ] 39. [web][test] **(perf R7.14, R13.8/17)** Lazy Gantt: dynamically import (`next/dynamic`) the Gantt view + ECharts/renderer, idle-prefetch for HR/Admin on project detail; for large projects compute Gantt layout (bar positions, milestone-diamond coordinates, dependency link paths) in a web worker; render milestone diamonds on a single SVG/canvas layer (not one DOM node per diamond). Test: bundle analyzer shows ECharts+Gantt out of main chunk; large-project layout asserts no main-thread task >50ms; milestone render asserts bounded DOM nodes + 60 FPS.
- [ ] 40. [web][test] **(perf R7.9, R13.12/11)** Isolated project work timer (same pattern as Attendance Phase 5): drive HH:MM:SS with `requestAnimationFrame` (1s-interval fallback), hold state in a ref/context outside the component tree (NOT global Zustand), recompute from local `task_timers` baseline so it survives navigation and is correct offline. Test: React Profiler asserts the project workspace + board render 0 times per timer tick; navigation test asserts the timer keeps counting across board↔list↔Gantt tab switches; 60 FPS / no main-thread jank.
- [ ] 41. [web][test] **(perf R7.6, R13.8/14/19)** Lazy Tiptap + virtualized comments + optimistic post: dynamically import the Tiptap editor (`next/dynamic`); virtualize the comment thread >100 (`@tanstack/react-virtual`, `React.memo` rows, stable key `comment_id`); post a comment optimistically, remove it on server error + danger toast (Timestamp conflict ordering on sync). Test: bundle analyzer shows Tiptap out of main chunk; render-count test asserts existing comments do not re-render on a new post; optimistic-rollback test (inject error → comment removed + toast).
- [ ] 42. [api][test] **(perf R7.10, R13.17)** Recurring-task regeneration queued: marking a recurring task done dispatches a queued job that generates the next occurrence, writes `task_recurrence_log`, and notifies HR (never inline). Test: queue dispatch test asserts the regeneration job is pushed (not run inline); feature test asserts the next occurrence + HR notification are produced by the job.
- [ ] 43. [web][test] **(perf R7.11, R13.24/17/19)** Quick Task Assignment ≤2-click + instant appear + queued notify: dashboard → fill → assign in ≤2 clicks with no full reload; the task appears instantly in the assignee's list on submit (optimistic, removed on error + danger toast); the Global Chat auto-notify message is dispatched from a queued job on completion. Test: click-count test asserts ≤2 clicks dashboard→assigned; optimistic-appear test asserts the task shows before the server responds; queued-notify test asserts the Global Chat message job is pushed on completion.
- [ ] 44. [api][web][test] **(perf R7.x, R13.20)** Offline Version+Merge queue for Tasks: each queued edit carries base `version` + generated `client_id` (idempotency key); on sync merge non-conflicting fields, return 409 + server copy only on same-field conflict (per-field merge affordance); Kanban drag, inline edit, comment authoring, and timer start/pause/resume queue + replay; offline banner (R11.6). Test: replay a queued edit twice with the same `client_id` → server applies exactly once; Version+Merge test asserts non-conflicting merge + 409 on same-field conflict; offline-banner test asserts the banner shows while disconnected.
- [ ] 45. [test] api feature tests (project CRUD + archive/delete, team auto-grant/revoke, sort, task create/assign/scope resolution, dependencies block+clear, comments, activity log, kanban reorder + status, QA builder + submission, timer seconds, recurrence generate + toggle, quick-task create + Global Chat notify, task submit/approve/redo, project submit/approve/redo, Admin sees all, history aggregate, self-create permitted vs denied, My Tasks private + HR-assign, saved views, pins, capability gate, offline Version+Merge + Server Validation + Timestamp); web/component tests (kanban dnd-kit cross+within reorder, inline edit, progress animate, saved view re-apply, pin sidebar, Gantt bars+diamonds+links, offline drag queue + reconcile, blocked-card non-drop); e2e (two-client live board, submit→approve, recurring→next, quick-task→Global Chat message).
- [ ] 46. [deploy] Staging deploy; smoke as karthik/Admin (all projects, Gantt, approve task+project), aravind/HR (create project + team auto-access, QA builder, approve), praveen/Employee (My Tasks, submit task with QA, project timer, progress update); verify live two-client Kanban, recurring auto-recreate, Quick Task → Global Chat notify, offline edit + reconnect merge; promote production.
- [ ] 47. [docs] Archive Phase 7 via `/opsx:archive`; update tracker ✅.
