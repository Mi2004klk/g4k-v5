# Tasks — leave-approvals

- [ ] 1. [spec] Extend OpenAPI: leave-request CRUD (create, list, detail, history), approvals pending queue, `POST /approvals/{id}/decision`, `GET /holidays`. Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `LeaveClient` and `ApprovalClient` methods in the shared API client.
- [ ] 3. [api] Migration: generic polymorphic `approvals` table (approvable_type, approvable_id, status submitted/pending/approved/rejected, submitted_by, current_approver_role, decision, decision_reason, decided_by, decided_at, payload jsonb) + indexes.
- [ ] 4. [api] Migration: `leave_requests` (user_id, start_date, end_date, reason, type, approval_id fk) + unique-partial on pending overlapping; read-only `holidays` table (seeded/owned by Phase 10).
- [ ] 5. [api] `ApprovalService`: `submit`/`approve`/`reject` with guarded state machine (pending→approved/rejected), per-type capability + routing-rule config map, terminal-state rejection. Only writer to `approvals`.
- [ ] 6. [api] Emit `ApprovalSubmitted` + `ApprovalDecided` Laravel events on each transition; listeners wire to notification fan-out, realtime broadcast, and audit row.
- [ ] 7. [api] Reuse test: submit/decide a throwaway approvable type through `ApprovalService` to prove Phase 7 plugs in with no new pipeline.
- [ ] 8. [api] `POST /leave-requests`: validate dates/type/reason; create `leave_requests`; open approval via `ApprovalService::submit` (employee→HR routing, HR self-request→Admin routing). Capability `employee.leave.request-self`.
- [ ] 9. [api] `GET /leave-requests` + `/leave-requests/{id}` + `/leave-requests/history`: scoped lists (employee own, HR team+own, Admin all) with filter bar (status, date range, type, person) + pagination (default 20).
- [ ] 10. [api] `POST /approvals/{id}/decision`: capability-gated by `current_approver_role` + type capability map (`hr.leave.approve-employee` / `admin.leave.approve-hr`); records decision, reason, decider, decided_at; emits `ApprovalDecided`.
- [ ] 11. [api] `GET /approvals/pending`: role-scoped pending queue (HR → employee leave; Admin → HR leave).
- [ ] 12. [api] `GET /holidays` (read-only) + `?year=` for the calendar view; data sourced from `holidays` managed in Phase 10.
- [ ] 13. [api] Realtime: broadcast `approval-status-change` on `private-user.{submitted_by}` on every transition via Reverb.
- [ ] 14. [api] Minimal notification stub: `notifications` table (id, user_id, type, title, body, read_at, link, timestamps) + `NotificationService` write + broadcast `notification-created` on `private-user.{id}`; consumed by bell + Notification Center; Phase 8 supersedes writers, keeps contract.
- [ ] 15. [api] Offline: queue `leave-request.create` via Offline Engine; replay with Server-Validation conflict strategy (server wins); disable decide while offline.
- [ ] 16. [seed] Seeder: sample leave requests across roles (employee→HR pending/approved/rejected, HR→Admin) + ensure `holidays` seed rows exist for the calendar.
- [ ] 17. [web] Employee leave request UI: date-range pick, type select, reason, required-field validation, submit loading, success toast; status visible on the request detail.
- [ ] 18. [web] HR leave request UI: same form; submission routes to Admin (labeling reflects the approver).
- [ ] 19. [web] Approve/reject flows: HR→employee and Admin→HR decision dialogs, reason required on reject, confirmation dialog, capability-gated buttons, instant status.
- [ ] 20. [web] Leave history + status badges (Amber=Pending, Green=Approved, Red=Rejected) with filter bar + pagination; scoped per role (own / team+own / all).
- [ ] 21. [web] Holiday calendar view rendering seeded holidays read-only; click a holiday for name/description.
- [ ] 22. [web] Notification hooks: bell increments and lists approval notifications; Notification Center shows submit/decision entries; subscribe to `private-user.{id}` for live updates.
- [ ] 23. [web] Capabilities: route guards + button visibility for `employee.leave.request-self`, `hr.leave.approve-employee`, `admin.leave.approve-hr`; offline submit queued state + decide disabled under offline banner.
- [ ] 24. [test] API feature tests (submit/routing/approve/reject/wrong-role-denial/terminal-state/pending-queue/history-scoping/holidays-read-only/offline-sync-conflict) + framework-reuse test; web tests (form validation, decision flows, badge colors, history filters, calendar, notification stub, offline states).
- [ ] 25. [deploy] Staging deploy; smoke a full cycle per role (employee submits→HR approves; HR submits→Admin rejects); verify bell + Notification Center entries and offline submit/decision-disabled; promote production.
- [ ] 26. [perf] Optimistic approve/reject + rollback: the dashboard notification and leave list expose a one-click Approve/Reject (1 click + confirm; reason required on reject) that flips the status badge instantly and removes the row from the pending queue before the server responds; on server error roll back to the prior Pending state + danger toast; disable + dot-loader to prevent double-submit. Test: optimistic-flip test + rollback-on-error test + double-submit test. (R6.5/6.6, R13.19, R13.24)
- [ ] 27. [perf] Composite indexes + query-count tests: add indexes on `leave_requests (user_id, status)`, `leave_requests (start_date)`, `approvals (status, submitted_at)` (pending-queue ordering) alongside the existing `(approvable_type, approvable_id)` and `(status, current_approver_role)`; assert `GET /leave-requests`, `/leave-requests/history`, `/approvals/pending` each execute ≤5 SQL with zero N+1 (via `DB::enableQueryLog()`) and p95 ≤200ms read at 10k rows; assert cursor pagination (never OFFSET); EXPLAIN-assert index usage. (R13.4/5/6)
- [ ] 28. [perf] Virtualize leave history: render leave history + pending-queue lists with `@tanstack/react-virtual` above 100 rows; `React.memo` rows with stable keys (`approval_id`); assert INP ≤200ms and 60 FPS at 5000 rows and that a single `approval-status-change` broadcast re-renders only the affected row (React Profiler render-count test). (R13.14/12)
- [ ] 29. [perf] Cached lists (30s) + realtime render-storm prevention: set TanStack Query `staleTime: 30s` for leave list/history/pending-queue (stale-while-revalidate on revisit, no spinner); cache `GET /holidays` 1h and lazy-load the holiday calendar via `next/dynamic`; on `approval-status-change` patch only the affected cached row + memoized row, never a blanket invalidate. Test: stale-then-fresh nav test + render-count test on broadcast. (R13.3/8/10/12)
- [ ] 30. [perf] One-click approval workflow + click-count test: wire dashboard notification → Approve/Reject (1 click + confirm) and leave list → Approve/Reject (1 click + confirm); verify the decision + notification fan-out split — decision is a fast synchronous DB write (guarded transition + audit row), only notification/realtime/audit listeners queue. Test: Playwright click-count test asserts ≤2 clicks and no full reload for approve/reject, submit-leave, view-history, open-calendar. (R6.5/6.6/6.8, R13.17/24)
- [ ] 31. [docs] Archive Phase 6 via `/opsx:archive`; update tracker ✅.
