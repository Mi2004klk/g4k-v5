# Tasks — app-shell

> Ordered, chunked (~2h each), tagged. Each phase ends with test + deploy + archive.

- [ ] 1. [ui-pkg] Author design tokens (color incl. brand purple `#9d00ff`/magenta/gold `#ffd700`, spacing 4px scale, type scale, elevation, motion durations per §10/§22) as Tailwind v4 CSS variables + TS constants in `packages/ui`; export barrel.
- [ ] 2. [ui-pkg][web] Theme engine: `themeStore` (Zustand) for mode + density; apply via `data-theme`/`data-density`; SSR-safe no-flash inline script; persist to `localStorage`. (Density: compact/comfortable.)
- [ ] 3. [api] Migration: `users.theme_mode`, `users.density`; create `pins` table (user_id, type, target_id, label, href, icon, unique). Seed-safe.
- [ ] 4. [spec] Extend OpenAPI: `GET/POST/DELETE /pins`, `PUT /me/preferences`; lint green; regenerate `packages/ui/src/types/api.ts`.
- [ ] 5. [api] `pins` CRUD (authz: own pins; `pins.manage`); `PUT /me/preferences` persists theme_mode/density (`preferences.update`). Wire capability gates from Phase 2.
- [ ] 6. [ui-pkg] Component library build-out: `Button` (variants incl. destructive-red, loading dot-state), `Card` (gradient header option), `Badge` + `StatusBadge` (Gray/Blue/Amber/Green/Red).
- [ ] 7. [ui-pkg] `Table` wrapper (TanStack Table): virtualization hook, column visibility, custom columns, saved-views hook. Demo with sample data.
- [ ] 7a. [ui-pkg][perf] Bake virtualization into the shared `Table` for lists > 100 rows (`@tanstack/react-virtual`: DOM nodes = visible + overscan, 60 FPS at 5000 rows); memoize rows via `React.memo` + stable keys; add a render-count test on a 1000-row table asserting no render storm. (R13.12/13.14, P-VIRTUAL/RERENDER)
- [ ] 8. [ui-pkg] Overlay primitives: `Dialog`, `ConfirmDialog` (destructive=red, Enter/Esc), `Drawer` (180–250ms motion), `Tooltip` (icon buttons + truncation).
- [ ] 8a. [ui-pkg] Disclosure primitives: `Sheet` (sides, mobile nav/filters), `Popover` (pickers/mention/avatar menu), `DropdownMenu` + `ContextMenu` (row actions, right-click bulk — R11.8), `Tabs` (lazy-mount content), `Collapsible`/`Accordion` (advanced options, filter sections), `ScrollArea`. All per COMPONENT-SYSTEM.md §2.
- [ ] 8b. [ui-pkg] Form primitives: `Input`/`Textarea`/`PasswordInput` (show/hide, error variant), `Select` (native on mobile), `Checkbox`/`Switch`/`RadioGroup`/`Slider`, `DatePicker`/`DateRangePicker` (native on mobile), `FileUpload` popup (format/size limits, preview — R11.3), `Form` suite (RHF+Zod) + `useFormDraft` (IndexedDB autosave 30s + restore banner — R3.7). Per COMPONENT-SYSTEM.md §1.
- [ ] 8c. [ui-pkg] Data/feedback primitives: `Avatar`/`AvatarGroup` (aspect-ratio reserved), `Progress` (animate 0→value), `Separator`, `Combobox`/`Autocomplete` (Command+Popover, debounced 250ms — R13.15), `Pagination` (cursor, 20/50/100), `Breadcrumb`, `Toast`(Sonner)/`NotificationsBell` shell/`AnnouncementCard` shell/`OfflineBanner`. Per COMPONENT-SYSTEM.md §3/§5/§6.
- [ ] 8d. [ui-pkg] Shared composites: `FilterBar` (search+popovers+date range+comboboxes+sort+direction+ClearAll+removable chips; mobile→Sheet — R3.8), `EmptyState` (real copy + illustration/animated-logo + optional action — R3.13), `Skeleton` shape primitives.
- [ ] 8e. [ui-pkg] Lazy wrappers: `TiptapEditor` + `Chart`(ECharts) wrappers, dynamically imported (R13.8) with token-mapped light/dark themes.
- [ ] 8f. [test][perf] Component verification: axe-core on every primitive (zero critical/serious — R13.23); visual regression at 360/768/1024/1440 (R13.22); focus-visible ring + keyboard nav (Esc/Enter/arrows) per COMPONENT-SYSTEM §0.
- [ ] 9. [ui-pkg] Feedback primitives: `Skeleton` shapes, `EmptyState` (illustration + animated logo mp4 slot + copy + action), `Toast` (top-right, 4s auto-dismiss + X, green/red/amber/blue) backed by `toastStore` + `useToast()`.
- [ ] 10. [ui-pkg] `CommandPalette` (cmdk; Ctrl+K; stub command list — full search R11.2 deferred) and `ShortcutOverlay` (Ctrl+/ help listing all shortcuts).
- [ ] 10a. [ui-pkg][perf] Command palette instant search: client-side fuzzy index paints results < 50ms per keystroke; structure indexing so it can move to a web worker if the set grows (no main-thread task > 50ms). Add a type-then-result-latency test. (R13.17, P-ASYNC-FS)
- [ ] 11. [web] App shell layout: top bar (search stub, bell slot, profile menu w/ theme+density toggle + device-sessions link), main content outlet, responsive container.
- [ ] 11a. [web][perf] Reserved-dimension CLS prevention: reserve sidebar width via CSS before hydration; use fixed-height skeletons and `aspect-ratio` boxes for avatars/images so CLS ≤ 0.1 on shell routes. (R13.2, P-CLS)
- [ ] 12. [web] Role-aware sidebar per spec §9: render Admin/HR/Employee trees, each entry capability-gated by active role; collapsible; active-route highlight. State in `shellStore`.
- [ ] 12a. [web][perf] Zustand slice-selector audit: confirm `themeStore`/`shellStore`/`toastStore`/`dialogStore`/`filterStore` subscribe via slice selectors (no whole-store subscriptions, no server data in Zustand); add a React DevTools Profiler check that theme toggle does not remount the tree. (R13.11, P-STATE)
- [ ] 13. [web] Mobile nav: bottom nav ≤5 icons (role-tuned) + hamburger full-screen menu containing remaining items; reserve fixed-bottom-input pattern for chat (Phase 8).
- [ ] 13a. [web][perf] Route lazy-loading + bundle budget: dynamic `import()` per in-app route; dynamically import + idle-prefetch heavy components (Tiptap editor, ECharts, command-palette index); wire `@next/bundle-analyzer` budget test asserting First-Load JS ≤ 200KB gz / route chunk ≤ 350KB gz (breach fails build). (R13.7/13.8, P-BUNDLE/LAZY)
- [ ] 14. [web] Breadcrumbs: derive from route tree; render on detail screens; each crumb navigates to its parent route.
- [ ] 15. [web][ui-pkg] Pinned items engine: star/pin control on project/task/profile screens → `POST /pins`; sidebar Pinned section lists + removes (TanStack Query `['pins']`); offline queue via Offline Engine.
- [ ] 16. [ui-pkg][web] Form system: React Hook Form + Zod `<Form>` + field primitives; required markers; `onBlur` validation + inline errors; submit loading dot-state + success toast (bottom-right); `FormSection` collapsibles.
- [ ] 17. [ui-pkg][web] Save-as-Draft + 30s autosave: snapshot drafts to IndexedDB (Offline Engine storage); restore banner on mount when draft is newer; clear draft on submit.
- [ ] 18. [ui-pkg] Reusable `FilterSortBar`: search (debounced 200ms), status multi-select, date range, dept/team, priority, sort field+direction, clear-all, removable chips; per-list filter state.
- [ ] 19. [ui-pkg][web] Inline editing (pencil → Enter commit / Escape cancel); icon-button hover states + tooltips; `ConfirmDialog` wiring for destructive actions.
- [ ] 20. [ui-pkg][web] dnd-kit list reordering (`SortableContext`/`useSortable`) + board scaffolding (`DndContext`); persist via Offline Engine. (ADR-007 — never mix with React Grid Layout.)
- [ ] 21. [ui-pkg] Pagination control: default 20 rows, options 50/100; integrate with TanStack Table + virtualization.
- [ ] 22. [web] Keyboard shortcuts: Ctrl+K palette, Ctrl+N context-new, Ctrl+/ overlay, Esc close, Enter submit/confirm; global hotkey provider + per-context bindings.
- [ ] 23. [ui-pkg] Loading affordances: progress bar animates 0→value; finalize button dot-state; skeleton compositions per surface; cached/partial content over spinners (R3.16).
- [ ] 24. [web][deploy] PWA: `manifest.webmanifest` (icons incl. king/crown, theme_color `#9d00ff`, standalone); register service worker (workbox/next-pwa) precaching shell + stale-while-revalidate GETs; verify installable.
- [ ] 24a. [web][perf] Asset optimization: self-host + subset fonts (latin + needed glyphs), `font-display: swap`, preload, ≤ 2 families; route all shell brand/logo images through `next/image`; add a CI check forbidding un-`next/image` `<img>`. (R13.9, P-IMG/FONT)
- [ ] 25. [web] Offline banner via `useConnectivity()` (Offline Engine monitor); "You're offline…"; forms queue + replay on reconnect (R11.6).
- [ ] 26. [seed] Seed a few sample pins per demo user (Admin/HR/Employee) so the Pinned section is exercised; verify no duplicate-pin constraint trips.
- [ ] 27. [test] ui-pkg unit/RTL tests (tokens, theme persist, Button dot-state, StatusBadge colors, Toast dismiss, ConfirmDialog destructive, FilterSortBar chips, palette Ctrl+K, inline edit, progress bar animate, dnd reorder). Web tests (role trees, capability-gating, mobile nav ≤5, breadcrumbs, pin reflect, form validation + restore banner, offline banner, SW+manifest).
- [ ] 27a. [test][perf] Lighthouse CI on shell routes (dashboard shell, a sample list, a sample form) in PRs enforcing LCP ≤ 2.5s / CLS ≤ 0.1 / INP ≤ 200ms / First-Load JS ≤ 200KB gz + font+image audits green; axe-core step asserting zero critical/serious WCAG 2.1 AA violations. Breaches tracked in TRACKER with owner + plan. (R13.1/13.2/13.23/13.29, P-REGRESS/A11Y)
- [ ] 28. [test] api feature tests (pins CRUD authz, preferences persist, capability gate denies). Verify lint + build + openapi lint green on CI.
- [ ] 29. [deploy] Staging deploy; smoke shell across all 3 roles (sidebar trees, mobile nav, theme toggle roam, pins, command palette, shortcuts, offline banner, PWA install prompt). Promote production.
- [ ] 30. [docs] Archive Phase 3 via `/opsx:archive`; update tracker ✅.
