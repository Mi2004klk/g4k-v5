# Tasks — attendance

- [ ] 1. [spec] Extend OpenAPI: attendance endpoints (clock-in, start-break, end-break, clock-out, me/today, me/history, me/day/{date}, admin/overview, admin/day, hr/today, hr/graph, correct, export). Lint green.
- [ ] 2. [ui-pkg] Regenerate API types; add `AttendanceClient` methods in shared API client.
- [ ] 3. [api] Migration: create `attendance_days`, `attendance_events`, `attendance_corrections`, `work_schedules`; indexes on `(user_id,date)`, `(date)`, `(status,date)`, `(user_id,timestamp)`, `(client_id)`; unique `(user_id,date)`.
- [ ] 4. [api] Reconciliation service: rebuild an `attendance_days` row from ordered, `client_id`-deduped `attendance_events`; compute total/break/overtime seconds, status (present/absent/late), late_minutes.
- [ ] 5. [api] `POST /attendance/clock-in|start-break|end-break|clock-out`: validate ordering, write event (idempotent on `client_id`), re-reconcile the day, return day + event.
- [ ] 6. [api] Capability middleware wiring: `employee.clock-self`, `hr.view-team-attendance`, `admin.view-all-attendance`, `admin.correct-attendance` on the matching routes.
- [ ] 7. [api] `GET /attendance/me/today`, `me/history`, `me/day/{date}`: open shift + timer baseline + heatmap summaries + clicked-day detail (projects/tasks).
- [ ] 8. [api] Overtime calculation: `overtime_seconds = max(0, total_seconds − standard_seconds)` from `work_schedules`; late badge when `clock_in > start_time`.
- [ ] 9. [api] `GET /attendance/admin/overview` + `admin/day`: company-wide rows with date/department/user/status filters (paginated, eager-loaded, N+1-safe); full summary for any person/date.
- [ ] 10. [api] `GET /attendance/hr/today` (present/absent/late/leave + filter) and `GET /attendance/hr/graph` (weekly/monthly series per user).
- [ ] 11. [api] `POST /attendance/correct`: Admin/HR edit; write `attendance_corrections` audit row (field, old/new, reason, corrected_by); re-reconcile the day.
- [ ] 12. [api] `GET /attendance/export`: stream xlsx (spout/Maatwebsite) scoped by the same filters; one row per user/date with totals/break/overtime/status/late.
- [ ] 13. [api] Realtime: broadcast `AttendancePresenceUpdated` on `presence-attendance-today` on every clock/break event; `AttendanceDayReconciled` on `private-user.{id}` after reconciliation.
- [ ] 14. [api] Scheduler jobs: `Attendance\RemindShiftStart` (start − lead min, default 15) and `Attendance\AlertMissedClockIn` (start + alert min, default 30); both read configurable times from `attendance_settings`; register in `routes/console.php` / scheduler.
- [ ] 15. [seed] Seeder: `work_schedules` default schedule (start/end/break/standard_seconds/working_days) tied to seeded working days; sample attendance days + events for a couple of employees (present/late/overtime) so heatmap/graph/correction have data.
- [ ] 16. [web] Clock In / Start Break / End Break / Clock Out controls; full shift timeline rendered from events; offline-queue path via the Offline Engine with `client_id`.
- [ ] 17. [web] Live HH:MM:SS timer: count-up, continues across navigation (Zustand UI state + local event baseline), turns amber on overtime, stops only on explicit Clock Out; reacts to `AttendanceDayReconciled`.
- [ ] 18. [web] Personal calendar heatmap history (ECharts calendar) with overtime distinct color; click a date → per-day summary (clock-in, breaks, clock-out, total/break/overtime, projects/tasks).
- [ ] 19. [web] Admin company-wide attendance view + filter bar (date/dept/person/status from Phase 3); virtualized list; click any date/person → full summary.
- [ ] 20. [web] HR today view (present/absent/late/leave filter) + weekly/monthly per-employee graphs (ECharts line/bar); link to an employee's leave requests.
- [ ] 21. [web] Manual correction dialog (Admin/HR): edit fields, require reason, show audit trail; capability-gated.
- [ ] 22. [web] Late badge component + overtime styling reused from Phase 3 status-badge tokens; live "who's clocked in now" widget subscribing to `presence-attendance-today` with polling fallback.
- [ ] 23. [web] Excel export action wired to `/attendance/export` with the current filters; download via blob; loading state.
- [ ] 24. [web] Offline: timer + events run from IndexedDB; on reconnect push queued events with `client_id`, accept server-reconciled day, surface conflicts as toasts (Server-Validation strategy).
- [ ] 25. [web][test] **(perf R5.14/13.11/12)** Build the live timer as an isolated component: drive it with `requestAnimationFrame` (1s-interval fallback), hold its state in a ref/context outside the component tree (NOT global Zustand), so each tick re-renders only the timer. Test: React Profiler asserts the dashboard shell + sibling widgets render 0 times per timer tick; navigation test asserts the timer keeps counting across route changes; assert 60 FPS / no main-thread jank.
- [ ] 26. [web][test] **(perf R5.13/13.24/19/22)** One-tap dashboard attendance widget: single-tap Start/Pause/End (≤2 clicks, no modal/form/full reload); mobile full-width green Start button ≥48px; tap disables the button + dot-loader to prevent double-submit; optimistic confirmation with rollback + danger toast on server error. Test: click-count test (≤2 clicks per workflow) + rollback test (inject server error → assert revert + danger toast).
- [ ] 27. [api][web][test] **(perf R5.15/16, R13.5/6)** Composite indexes on `attendance_days (user_id,date)`, `(date,status)`, `(department join, date)` and `attendance_events (user_id,timestamp)`, `(client_id)`; cursor pagination on overview/today/history/graph endpoints. Test: query-count test asserts ≤5 SQL per list request and zero N+1 (via `DB::enableQueryLog()`) at 10k rows; EXPLAIN asserts index usage on filtered/ordered columns.
- [ ] 28. [web][test] **(perf R5.15, R13.14/12)** Virtualize the personal-history, Admin company-wide, and "today's attendance" lists with `@tanstack/react-virtual` (>100 rows); `React.memo` rows with stable keys (`attendance_day_id`); no anonymous callbacks in props. Test: render 5000 rows, assert DOM nodes ≤ (visible + overscan) and 60 FPS / INP ≤200ms.
- [ ] 29. [api][web][test] **(perf R5.16, R13.3/10/15)** Today/overview views: TanStack Query `staleTime: 30s` (stale-while-revalidate, no spinner on revisit); status/department filters update in place (URL + cache, no reload) with 250ms debounce; realtime `presence-attendance-today` keeps the cached list live. Test: cache-hit renders immediately on revisit; in-place filter test asserts no full reload; server p95 ≤200ms benchmark test.
- [ ] 30. [web] **(perf R13.8/17)** Calendar heatmap (ECharts) dynamically imported via `next/dynamic` + idle-prefetched when history is likely next; fetch per-day data on month change and cache per month in TanStack Query; if the heatmap/graph render exceeds 50ms main-thread work, move ECharts to a web worker / chunk it so INP is never blocked.
- [ ] 31. [api][web] **(perf R5.10, R13.17/4)** Excel export: estimate request cost; route exports >500ms to a queue job that streams the xlsx; UI shows a queued/progress state and the download is streamed (no long blocking PHP request).
- [ ] 32. [web][test] **(perf R5.12, R13.20)** Offline clock path: timer + events run from IndexedDB; each action queued with a generated `client_id` idempotency key; offline banner shown while disconnected. Test: replay a queued action twice with the same `client_id` → server applies it exactly once; reconnect sync replaces local state with the reconciled day.
- [ ] 33. [test] api feature tests (clock ordering, reconciliation, overtime, late badge, idempotency, correction audit, capability gate, export rows, conflict/overlap/late-arrival reconciliation, scheduler timing); web tests (timer survives nav + amber overtime, heatmap overtime color, filters, offline reconcile, correction).
- [ ] 34. [test] **(perf R13.29)** Performance-budget gate for the attendance routes: Lighthouse CI asserts LCP≤2.5s / INP≤200ms / CLS≤0.1; bundle analyzer asserts history (heatmap) route First-Load JS ≤200KB gz (heatmap kept out of main chunk); render-count + query-count tests from tasks 25/27/28 run in CI; a regression fails the build.
- [ ] 35. [deploy] Staging deploy; smoke clock in/out + break + offline sync as an employee; verify Admin overview + HR today/graph + correction + export + scheduler fires; promote production.
- [ ] 36. [docs] Archive Phase 5 via `/opsx:archive`; update tracker ✅.
